# Kore 2.0 实施计划

**创建日期**: 2026-01-17
**版本**: 2.0.0-dev
**预计完成**: 22-27 周

---

## 目录

1. [实施路线图](#实施路线图)
2. [Phase 0: 基础设施](#phase-0-基础设施)
3. [Phase 1: Agent 核心](#phase-1-agent-核心)
4. [Phase 2: Environment Manager](#phase-2-environment-manager)
5. [Phase 3: LSP Manager](#phase-3-lsp-manager)
6. [Phase 4: Session Manager](#phase-4-session-manager)
7. [Phase 5: Event System](#phase-5-event-system)
8. [Phase 6: 扩展与市场](#phase-6-扩展与市场)
9. [Phase 7: GUI 工作台](#phase-7-gui-工作台)
10. [Phase 8: TUI 客户端](#phase-8-tui-客户端)
11. [Phase 9: 测试与优化](#phase-9-测试与优化)
12. [发布计划](#发布计划)

---

## 实施路线图

```
Phase 0: ████████████░░░░░░░░░░░░░  基础设施（1-2 周）
Phase 1: ████████████░░░░░░░░░░░░░  Agent 核心（2-3 周）
Phase 2: ████████████░░░░░░░░░░░░░  Environment Manager（2 周）
Phase 3: ░░░░░░░░░░░░░░░░░░░░░░░░░  LSP Manager（3-4 周）
Phase 4: ░░░░░░░░░░░░░░░░░░░░░░░░░  Session Manager（2-3 周）
Phase 5: ░░░░░░░░░░░░░░░░░░░░░░░░░  Event System（2 周）
Phase 6: ░░░░░░░░░░░░░░░░░░░░░░░░░  扩展与市场（2-3 周）
Phase 7: ░░░░░░░░░░░░░░░░░░░░░░░░░  GUI 工作台（3 周）
Phase 8: ░░░░░░░░░░░░░░░░░░░░░░░░░  TUI 客户端（3 周）
Phase 9: ░░░░░░░░░░░░░░░░░░░░░░░░░  测试与优化（2 周）
```

**总计**: 22-27 周（约 5-7 个月）

**进度**: Phase 0-2 已完成（共 5-7 周）

---

## Phase 0: 基础设施

**时间**: 1-2 周
**状态**: 🔵 进行中
**目标**: 搭建开发环境和核心基础设施

### 任务清单

#### 0.1 项目结构重构

**新目录结构**：
```
kore-foundation/
├── cmd/
│   ├── kore/               # 客户端二进制
│   │   └── main.go
│   └── kored/              # 服务器二进制
│       └── main.go
├── internal/
│   ├── server/             # 服务器核心（新增）
│   │   ├── server.go
│   │   └── grpc.go
│   ├── client/             # 客户端适配器
│   │   ├── tui/
│   │   ├── cli/
│   │   └── client.go
│   ├── rpc/                # gRPC 定义（新增）
│   │   └── kore.pb.go
│   ├── agent/              # Agent 核心
│   ├── environment/        # Environment Manager
│   ├── lsp/                # LSP Manager
│   ├── session/            # Session Manager
│   ├── eventbus/           # Event Bus
│   └── ...
├── api/
│   └── proto/              # Protocol Buffers（新增）
│       └── kore.proto
└── ...
```

**文件变更**：
- [ ] 创建 `internal/server/` 目录
- [ ] 创建 `internal/client/` 目录
- [ ] 创建 `api/proto/` 目录
- [ ] 移动 `cmd/kore/main.go` 到客户端入口
- [ ] 创建 `cmd/kored/main.go` 服务器入口

#### 0.2 Protocol Buffers 定义

**文件**: `api/proto/kore.proto`

```protobuf
syntax = "proto3";

package kore;

option go_package = "github.com/yukin/kore/internal/rpc";

// Kore 服务定义
service Kore {
  // 会话管理
  rpc CreateSession(CreateSessionRequest) returns (Session);
  rpc GetSession(GetSessionRequest) returns (Session);
  rpc ListSessions(ListSessionsRequest) returns (ListSessionsResponse);
  rpc CloseSession(CloseSessionRequest) returns (CloseSessionResponse);

  // 消息流（双向流）
  rpc SendMessage(stream MessageRequest) returns (stream MessageResponse);

  // 命令执行（流式输出）
  rpc ExecuteCommand(CommandRequest) returns (stream CommandOutput);

  // LSP 请求
  rpc LSPComplete(LSPCompleteRequest) returns (LSPCompleteResponse);
  rpc LSPDefinition(LSPDefinitionRequest) returns (LSPDefinitionResponse);
  rpc LSPHover(LSPHoverRequest) returns (LSPHoverResponse);

  // 事件订阅
  rpc SubscribeEvents(SubscribeRequest) returns (stream Event);
}

// 消息类型
message MessageRequest {
  string session_id = 1;
  string content = 2;
  string role = 3;  // "user", "assistant", "system"
}

message MessageResponse {
  string content = 1;
  string role = 2;
  int64 timestamp = 3;
  bool done = 4;
}

// 命令执行
message CommandRequest {
  string session_id = 1;
  string command = 2;
  repeated string args = 3;
  string working_dir = 4;
  map<string, string> env = 5;
}

message CommandOutput {
  enum OutputType {
    STDOUT = 0;
    STDERR = 1;
    EXIT = 2;
  }
  OutputType type = 1;
  bytes data = 2;
  int32 exit_code = 3;
}

// LSP 请求
message LSPCompleteRequest {
  string session_id = 1;
  string file_uri = 2;
  int32 line = 3;
  int32 character = 4;
}

message LSPCompleteResponse {
  repeated CompletionItem items = 1;
}

message CompletionItem {
  string label = 1;
  string kind = 2;
  string detail = 3;
  string documentation = 4;
  string insert_text = 5;
}

// 会话
message Session {
  string id = 1;
  string name = 2;
  string agent_type = 3;
  string status = 4;
  int64 created_at = 5;
  int64 last_active_at = 6;
}

// 事件
message Event {
  string type = 1;
  string session_id = 2;
  bytes data = 3;
  int64 timestamp = 4;
}
```

**任务**：
- [ ] 创建 `api/proto/kore.proto`
- [ ] 定义所有 RPC 接口
- [ ] 定义消息类型

#### 0.3 gRPC 代码生成

**任务**：
- [ ] 安装 Protocol Buffers 编译器
  ```bash
  # macOS
  brew install protobuf

  # Linux
  apt-get install protobuf-compiler
  ```
- [ ] 安装 Go 插件
  ```bash
  go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
  go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
  ```
- [ ] 生成代码
  ```bash
  protoc --go_out=. --go_opt=paths=source_relative \
         --go-grpc_out=. --go-grpc_opt=paths=source_relative \
         api/proto/kore.proto
  ```
- [ ] 验证生成的 `internal/rpc/kore.pb.go`

#### 0.4 gRPC Server 基础框架

**文件**: `internal/server/grpc.go`

```go
package server

import (
    "context"
    "net"

    "google.golang.org/grpc"
    "github.com/yukin/kore/internal/rpc"
)

type KoreServer struct {
    rpc.UnimplementedKoreServer
    sessionManager *session.Manager
    eventBus       *eventbus.EventBus
}

func NewKoreServer(sm *session.Manager, eb *eventbus.EventBus) *KoreServer {
    return &KoreServer{
        sessionManager: sm,
        eventBus:       eb,
    }
}

func (s *KoreServer) Start(listenAddr string) error {
    lis, err := net.Listen("tcp", listenAddr)
    if err != nil {
        return err
    }

    grpcServer := grpc.NewServer()
    rpc.RegisterKoreServer(grpcServer, s)

    return grpcServer.Serve(lis)
}

// 实现 RPC 接口（Phase 4-6 完成）
func (s *KoreServer) CreateSession(ctx context.Context, req *rpc.CreateSessionRequest) (*rpc.Session, error) {
    // TODO: Phase 4
    return nil, nil
}

// ... 其他 RPC 实现
```

**任务**：
- [ ] 创建 `internal/server/server.go`
- [ ] 创建 `internal/server/grpc.go`
- [ ] 实现 Server 启动逻辑
- [ ] 添加优雅关闭

#### 0.5 gRPC Client 基础框架

**文件**: `internal/client/client.go`

```go
package client

import (
    "context"
    "time"

    "google.golang.org/grpc"
    "github.com/yukin/kore/internal/rpc"
)

type KoreClient struct {
    conn   *grpc.ClientConn
    client rpc.KoreClient
}

func NewKoreClient(serverAddr string) (*KoreClient, error) {
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()

    conn, err := grpc.DialContext(ctx, serverAddr,
        grpc.WithTransportCredentials(insecure.NewCredentials()),
        grpc.WithBlock(),
    )
    if err != nil {
        return nil, err
    }

    return &KoreClient{
        conn:   conn,
        client: rpc.NewKoreClient(conn),
    }, nil
}

func (c *KoreClient) Close() error {
    return c.conn.Close()
}

func (c *KoreClient) CreateSession(ctx context.Context, name string) (*rpc.Session, error) {
    return c.client.CreateSession(ctx, &rpc.CreateSessionRequest{
        Name: name,
    })
}
```

**任务**：
- [ ] 创建 `internal/client/client.go`
- [ ] 实现 Client 连接逻辑
- [ ] 添加重连机制

#### 0.6 依赖管理

**任务**：
- [ ] 更新 `go.mod`
  ```go
  require (
      google.golang.org/grpc v1.62.0
      google.golang.org/protobuf v1.32.0
      modernc.org/sqlite v1.28.0
      github.com/sourcegraph/go-lsp v0.0.0
      github.com/sourcegraph/jsonrpc2 v0.2.0
      github.com/charmbracelet/bubbletea v0.25.0
      github.com/charmbracelet/lipgloss v0.9.0
      github.com/charmbracelet/bubbles v0.18.0
  )
  ```
- [ ] 运行 `go mod tidy`
- [ ] 验证所有依赖编译

#### 0.7 SQLite 数据库初始化

**文件**: `internal/storage/sqlite.go`

```go
package storage

import (
    "context"
    "database/sql"

    "modernc.org/sqlite"
)

type SQLiteStore struct {
    db *sql.DB
}

func NewSQLiteStore(dbPath string) (*SQLiteStore, error) {
    db, err := sql.Open("sqlite", dbPath)
    if err != nil {
        return nil, err
    }

    // 创建表
    if err := initSchema(db); err != nil {
        return nil, err
    }

    return &SQLiteStore{db: db}, nil
}

func initSchema(db *sql.DB) error {
    schema := `
    CREATE TABLE IF NOT EXISTS sessions (
        id TEXT PRIMARY KEY,
        name TEXT NOT NULL,
        created_at INTEGER NOT NULL,
        last_active_at INTEGER NOT NULL,
        agent_type TEXT NOT NULL,
        status TEXT NOT NULL,
        metadata JSON
    );

    CREATE TABLE IF NOT EXISTS messages (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        session_id TEXT NOT NULL,
        role TEXT NOT NULL,
        content TEXT NOT NULL,
        timestamp INTEGER NOT NULL,
        metadata JSON,
        FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
    );

    CREATE INDEX IF NOT EXISTS idx_messages_session_id ON messages(session_id);
    CREATE INDEX IF NOT EXISTS idx_messages_timestamp ON messages(timestamp);
    `

    _, err := db.Exec(schema)
    return err
}
```

**任务**：
- [ ] 创建 `internal/storage/` 目录
- [ ] 实现 `SQLiteStore`
- [ ] 编写单元测试

#### 0.8 配置系统重构

**文件**: `internal/config/config.go`

```go
package config

type Config struct {
    Server ServerConfig `yaml:"server"`
    Client ClientConfig `yaml:"client"`
    LLM    LLMConfig    `yaml:"llm"`
    DB     DBConfig     `yaml:"db"`
    Agents AgentsConfig `yaml:"agents"`
    Policy PolicyConfig `yaml:"policy"`
}

type ServerConfig struct {
    ListenAddr string `yaml:"listen_addr"`
    Mode       string `yaml:"mode"`  // "socket", "tcp", "auto"
}

type ClientConfig struct {
    ServerAddr string `yaml:"server_addr"`
    UI         string `yaml:"ui"`     // "tui", "cli"
}

type DBConfig struct {
    Path           string `yaml:"path"`
    EnableEncrypt  bool   `yaml:"enable_encryption"`
    EncryptionKey  string `yaml:"encryption_key_env"`
}

type AgentsConfig struct {
    Path string `yaml:"path"` // configs/agents.yaml
}

type PolicyConfig struct {
    Path string `yaml:"path"` // configs/policy.yaml
}
```

**任务**：
- [ ] 更新 `internal/config/config.go`
- [ ] 支持服务器/客户端分离配置
- [ ] 添加配置验证
- [ ] 提供默认的多模型编排配置（`configs/agents.yaml`）
- [ ] 提供默认的权限策略配置（`configs/policy.yaml`）

---

## Phase 1: Agent 核心

**时间**: 2-3 周
**状态**: ⏸️ 待开始
**目标**: 实现自我修正的 ReAct 循环引擎

### 关键任务

#### 1.1 Loop Controller 基础框架

**文件**: `internal/agent/loop.go`

- [ ] 定义 `AgentState` 枚举
- [ ] 定义 `LoopIteration` 结构
- [ ] 实现 `LoopController.Run()`
- [ ] 实现状态机转换逻辑

#### 1.2 上下文剪枝策略

**文件**: `internal/agent/context_strategy.go`

- [ ] 定义 `ContextStrategy` 接口
- [ ] 实现 `RollingWindowStrategy`
- [ ] 实现历史摘要化

#### 1.3 ReAct 循环实现

**文件**: `internal/agent/react.go`

- [ ] 实现 `Plan()` 阶段
- [ ] 实现 `Execute()` 阶段
- [ ] 实现 `Observe()` 阶段
- [ ] 实现 `Reflect()` 阶段

#### 1.4 Agent 类型系统

- [ ] 实现 Build Agent（完全访问）
- [ ] 实现 Plan Agent（只读）
- [ ] 实现 General Agent（复杂任务）

#### 1.5 多模型分工与编排

- [ ] 定义角色/模型映射配置（`configs/agents.yaml`）
- [ ] 增加编排层角色（Planner/Reviewer/Supervisor）
- [ ] 实现角色调度策略（先规划、并行执行、再评审）
- [ ] 角色级工具白名单与权限绑定
- [ ] 默认使用 OpenAI 作为规划/审查协助模型（可配置切换）
- [ ] 角色切换与降级策略（模型不可用时回退）
- [ ] 多角色并行执行的结果聚合与冲突提示
- [ ] 验收标准：任务树输出完整、角色执行可并行、Reviewer 可阻断合并

---

## Phase 2: Environment Manager

**时间**: 2 周
**状态**: ✅ 已完成 (2026-01-18)
**目标**: 实现安全的命令执行 + 文件操作

### 关键任务

#### 2.1 本地环境实现

**文件**: `internal/environment/local.go`

- [x] 实现 `Execute()`
- [x] 实现 `ExecuteStream()`（实时流）
- [x] 实现 `WriteFile()`
- [x] 实现 `ReadFile()`
- [x] 实现 `Diff()`

#### 2.2 长运行进程管理

- [x] 实现 `StartBackgroundProcess()`
- [x] 实现 `KillProcess()`
- [x] 实现进程监控

#### 2.3 安全拦截器

**文件**: `internal/environment/security.go`

- [x] 路径遍历防护
- [x] 命令注入防护
- [x] 危险命令黑名单

#### 2.4 虚拟文档支持

- [x] 实现内存文件系统
- [x] 实现虚拟文档 CRUD
- [x] 实现与 LSP 的集成

### 实施总结

Phase 2 已成功完成，实现了完整的 Environment Manager 系统，包括：

1. **本地环境管理** (`internal/environment/local.go`)
   - 命令执行（同步/流式）
   - 文件读写（带备份）
   - 文件差异比较
   - 工作目录管理

2. **安全拦截器** (`internal/environment/security.go`)
   - 三级安全策略（Strict/Standard/Permissive）
   - 路径遍历防护
   - 命令注入检测
   - 危险命令黑名单
   - 文件名验证
   - 环境变量清理

3. **虚拟文件系统** (`internal/environment/virtual.go`)
   - 内存中的文档管理
   - 文档创建/读取/更新/删除
   - 元数据支持
   - 提交/回滚机制
   - 统计信息
   - 目录分组

4. **测试覆盖**
   - 单元测试覆盖所有核心功能
   - 安全测试验证防护机制
   - 跨平台兼容性测试（Windows/Unix）

---

## Phase 3: LSP Manager

**时间**: 3-4 周
**状态**: ⏸️ 待开始
**目标**: 实现完整的 LSP 客户端

### 关键任务

#### 3.1 JSON-RPC 通信

**文件**: `internal/lsp/jsonrpc.go`

- [ ] 实现 JSON-RPC 2.0 客户端
- [ ] 实现 Stdio 通信
- [ ] 实现异步通知处理

#### 3.2 LSP 客户端基础

**文件**: `internal/lsp/client.go`

- [ ] 实现 `Initialize()`
- [ ] 实现 `Shutdown()`
- [ ] 实现 `DidOpen()`
- [ ] 实现 `DidChange()`

#### 3.3 语言特性实现

- [ ] 实现 `Complete()`
- [ ] 实现 `Definition()`
- [ ] 实现 `References()`
- [ ] 实现 `Hover()`
- [ ] 实现 `Rename()`

#### 3.4 诊断功能

- [ ] 实现 `PublishDiagnostics()`
- [ ] 实现实时错误推送

#### 3.5 LSP Manager 单例

**文件**: `internal/lsp/manager.go`

- [ ] 实现单例模式
- [ ] 实现服务器注册表
- [ ] 实现自动重启（崩溃恢复）

#### 3.6 语言支持

- [ ] Go (gopls)
- [ ] Python (pyright)
- [ ] TypeScript/JavaScript (tsserver)

---

## Phase 4: Session Manager

**时间**: 2-3 周
**状态**: ⏸️ 待开始
**目标**: 实现多会话管理

### 关键任务

#### 4.1 Session 数据结构

**文件**: `internal/session/session.go`

- [ ] 定义 `Session` 结构
- [ ] 实现会话生命周期

#### 4.2 Session Manager

**文件**: `internal/session/manager.go`

- [ ] 实现 `CreateSession()`
- [ ] 实现 `GetSession()`
- [ ] 实现 `ListSessions()`
- [ ] 实现 `CloseSession()`
- [ ] 实现 `SwitchSession()`

#### 4.3 持久化集成

- [ ] 实现 SQLite 存储适配器
- [ ] 实现会话保存/加载
- [ ] 实现消息流式读取

#### 4.4 可选加密

- [ ] 实现 AES-GCM 加密
- [ ] 实现透明加密层
- [ ] 实现密钥管理

---

## Phase 5: Event System

**时间**: 2 周
**状态**: ⏸️ 待开始
**目标**: 实现背压感知的事件总线

### 关键任务

#### 5.1 Event Bus 核心

**文件**: `internal/eventbus/bus.go`

- [ ] 实现 `EventBus` 结构
- [ ] 实现 `Subscribe()`
- [ ] 实现 `Publish()`
- [ ] 实现 `Unsubscribe()`

#### 5.2 背压处理

- [ ] 实现超时机制
- [ ] 实现缓冲配置
- [ ] 实现事件过滤器

#### 5.3 事件类型定义

- [ ] `llm_token` 事件
- [ ] `tool_output` 事件
- [ ] `status` 事件
- [ ] `session_*` 事件

#### 5.4 gRPC 流集成

- [ ] 实现 `SubscribeEvents` RPC
- [ ] 实现实时事件推送

---

## Phase 6: 扩展与市场

**时间**: 2-3 周
**状态**: ⏸️ 待开始
**目标**: 集成 Claude 技能机制并提供可安装的扩展市场

### 关键任务

#### 6.1 工具调用链集成（Skill Runtime）

- [ ] 定义 Skill 运行时接口与生命周期
- [ ] 将 Skill 解析与执行接入工具调用链
- [ ] 支持 Skill 权限与沙箱策略

#### 6.2 Skill 注册与发现

- [ ] 设计 Skill manifest 规范（名称、版本、入口、权限）
- [ ] 本地 Skill Registry（安装/启用/禁用）
- [ ] 版本校验与依赖检查

#### 6.3 MCP/Skill 插件安装器

- [ ] 支持 MCP 与 Skill 的统一安装入口
- [ ] 安装包签名/校验（最低限度校验）
- [ ] 卸载与回滚机制

#### 6.4 插件市场（最小可用）

- [ ] 扩展索引与搜索
- [ ] 一键安装/更新/移除
- [ ] 基础元信息展示（作者、权限、来源）

#### 6.5 工具权限模型与审计

- [ ] 定义角色-工具-作用域策略模型（路径/命令/API 资源）
- [ ] 实现 PolicyEngine 并接入 ToolExecutor
- [ ] 统一拒绝策略（默认拒绝 + 详细原因）
- [ ] 审计日志记录（允许/拒绝/参数摘要）
- [ ] 越权审批入口（人工或 Supervisor 代执行）

---

## Phase 7: GUI 工作台

**时间**: 3 周
**状态**: ⏸️ 待开始
**目标**: 以 CLI 为核心的可视化工作台，面向多类型创作工具接入

### 关键任务

#### 7.1 GUI 客户端框架

- [ ] GUI 客户端工程初始化（桌面/浏览器一体化方案评估）
- [ ] 与 gRPC 双向流对接（复用 CLI 协议）
- [ ] 会话/消息视图基础布局

#### 7.2 工具面板与能力中心

- [ ] 统一工具目录与搜索
- [ ] 工具详情与权限提示
- [ ] 快捷启用/禁用与常用工具固定

#### 7.3 创作工作区

- [ ] 画布/多面板布局（文本/图像/表格/文件预览）
- [ ] 结果资产管理（导出/版本/引用）
- [ ] 任务模板与预设（面向文案/设计/多媒体）

#### 7.4 CLI 核心联动

- [ ] CLI 指令与 GUI 操作双向同步
- [ ] CLI 任务可视化状态与日志
- [ ] 统一快捷键与命令面板

---

## Phase 8: TUI 客户端

**时间**: 3 周
**状态**: ⏸️ 待开始
**目标**: 实现多会话 TUI 界面

### 关键任务

#### 8.1 TUI 基础框架

**文件**: `internal/client/tui/model.go`

- [ ] 创建 Bubble Tea Model
- [ ] 实现 `Init()`
- [ ] 实现 `Update()`
- [ ] 实现 `View()`

#### 8.2 标签页组件

**文件**: `internal/client/tui/tab.go`

- [ ] 实现标签栏
- [ ] 实现标签切换（Ctrl+Tab）
- [ ] 实现未读提示
- [ ] 实现标签关闭（Ctrl+W）

#### 8.3 消息视口

**文件**: `internal/client/tui/viewport.go`

- [ ] 实现消息显示
- [ ] 实现滚动（Ctrl+↑/↓）
- [ ] 实现流式追加
- [ ] 实现代码高亮

#### 8.4 输入框

**文件**: `internal/client/tui/input.go`

- [ ] 实现文本输入
- [ ] 实现多行支持
- [ ] 实现历史记录（↑/↓）
- [ ] 实现快捷键（Ctrl+C 清空）

#### 8.5 状态栏

**文件**: `internal/client/tui/status.go`

- [ ] 实现状态显示
- [ ] 实现动画效果
- [ ] 实现详情切换（Ctrl+D）

#### 8.6 Client 集成

- [ ] 连接到 gRPC Server
- [ ] 实现消息发送
- [ ] 实现事件订阅
- [ ] 实现错误处理

---

## Phase 9: 测试与优化

**时间**: 2 周
**状态**: ⏸️ 待开始
**目标**: 完整测试覆盖 + 性能优化

### 关键任务

#### 9.1 单元测试

- [ ] Agent Loop Controller 测试
- [ ] Environment Manager 测试
- [ ] LSP Client Mock 测试
- [ ] Session Manager 测试
- [ ] Event Bus 测试

#### 9.2 集成测试

- [ ] gRPC Server/Client 测试
- [ ] 端到端工作流测试
- [ ] 多会话并发测试

#### 9.3 性能优化

- [ ] CPU Profiling
- [ ] Memory Profiling
- [ ] Goroutine Leaks 检测
- [ ] 优化热点代码

#### 9.4 文档完善

- [ ] API 文档
- [ ] 用户手册
- [ ] 开发指南
- [ ] 示例代码

---

## 发布计划

### v0.1.0-alpha (Phase 0 完成后)

**内容**：
- ✅ 基础架构搭建
- ✅ gRPC Server/Client 基础框架
- ✅ SQLite 数据库初始化

**里程碑**：可以建立 Server-Client 连接

### v0.2.0-alpha (Phase 1 完成后)

**内容**：
- ✅ Agent Loop Controller
- ✅ ReAct 循环实现
- ✅ 上下文剪枝策略

**里程碑**：可以执行简单的 Agent 任务

### v0.3.0-alpha (Phase 2 完成后)

**内容**：
- ✅ Environment Manager
- ✅ 虚拟文档支持
- ✅ 安全沙箱基础

**里程碑**：可以安全执行命令和文件操作

### v0.4.0-alpha (Phase 3 完成后)

**内容**：
- ✅ LSP Manager
- ✅ Go/Python/TypeScript 支持
- ✅ 代码补全、跳转、诊断

**里程碑**：具备 IDE 级别的代码理解能力

### v0.5.0-beta (Phase 4 完成后)

**内容**：
- ✅ Session Manager
- ✅ 多会话并行
- ✅ 持久化存储

**里程碑**：可以管理多个独立的对话

### v0.6.0-beta (Phase 5 完成后)

**内容**：
- ✅ Event Bus
- ✅ 实时流式体验
- ✅ 背压处理

**里程碑**：流畅的实时交互

### v0.7.0-beta (Phase 6 完成后)

**内容**：
- ✅ Skill Runtime
- ✅ MCP/Skill 插件安装器
- ✅ 插件市场（最小可用）

**里程碑**：可扩展生态闭环

### v0.8.0-beta (Phase 7 完成后)

**内容**：
- ✅ GUI 工作台基础
- ✅ 工具面板与能力中心
- ✅ CLI 核心联动

**里程碑**：可视化工作台可用

### v0.9.0-beta (Phase 8 完成后)

**内容**：
- ✅ TUI 多标签页
- ✅ 完整的用户界面
- ✅ CLI 客户端

**里程碑**：可用的 MVP 版本

### v1.0.0 (Phase 9 完成后)

**内容**：
- ✅ 完整测试覆盖
- ✅ 性能优化
- ✅ 完整文档

**里程碑**：生产就绪版本

---

## 开发规范

### Git 提交规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型**：
- `feat`: 新功能
- `fix`: 修复 bug
- `refactor`: 重构
- `test`: 测试
- `docs`: 文档
- `chore`: 构建/工具

**示例**：
```
feat(agent): implement loop controller

- Add AgentState enum
- Implement Run() method
- Add state machine transitions

Closes #123
```

### 代码审查清单

- [ ] 遵循 Go 代码规范
- [ ] 通过 `gofmt` 检查
- [ ] 通过 `go vet` 检查
- [ ] 单元测试覆盖率 > 80%
- [ ] 添加必要的文档注释
- [ ] 更新相关文档

---

## 风险与应对

### 技术风险

| 风险 | 影响 | 应对 |
|------|------|------|
| LSP 集成复杂度高 | 延期 1-2 周 | 先实现基础功能，高级特性分阶段 |
| 性能问题 | 用户体验差 | 提前进行性能测试，预留优化时间 |
| Go 依赖兼容性 | 编译失败 | 锁定依赖版本，定期更新 |

### 进度风险

| 风险 | 影响 | 应对 |
|------|------|------|
| 时间估算不准确 | 延期 | 预留 20% 缓冲时间 |
| 需求变更 | 返工 | 严格变更控制流程 |

---

## 参考资料

- [Kore 2.0 设计文档](./2026-01-17-kore-2.0-design.md)
- [Go gRPC 快速开始](https://grpc.io/docs/languages/go/quickstart/)
- [Bubble Tea 教程](https://github.com/charmbracelet/bubbletea/tree/master/tutorials)
- [LSP 规范](https://microsoft.github.io/language-server-protocol/)

---

**文档版本**: 1.0
**最后更新**: 2026-01-17
**维护者**: Kore Team
